<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp">

    <!-- Nama Budget -->
    <TextView
        android:id="@+id/txtBudgetKategori"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Nama Budget"
        android:textSize="16sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/txtNominalBudget"
        app:layout_constraintHorizontal_bias="0" />

    <!-- Nominal -->
    <TextView
        android:id="@+id/txtNominalBudget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rp 2.000.000"
        android:textSize="14sp"
        android:textColor="#555555"
        android:textAlignment="viewEnd"
        app:layout_constraintTop_toTopOf="@id/txtBudgetKategori"
        app:layout_constraintBottom_toBottomOf="@id/txtBudgetKategori"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
